package com.github.jaubuchon.seleniumutilities.section;

/**
 * Represent the current context inside the browser.
 * It searchs directly from the WebDriver and the context is allways valid.
 */
public class CurrentContext extends ApplicationSection{
}
